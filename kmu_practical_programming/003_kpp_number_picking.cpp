/***********************************************
 * 2025-04-13
 * 003_실전프로젝트-수고르기-std
 ***********************************************/
#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define endl '\n'

int main() {
    fastio;
    int t;
    cin >> t;
    while(t-- > 0){
        int n, m; // n개의 정수, 차이 m
        cin >> n >> m;
        vector<int> number(n);
        for(int i = 0 ; i < n; i++){
            cin >> number[i];
        }
        int start = 0, end = 0;
        int minus = 5, curr_minus = 0;
        if(){
                
        }else{
            현재 차이가 minus
        }
    }
    return 0;
}