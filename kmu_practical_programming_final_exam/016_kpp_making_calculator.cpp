/***********************************************
 * 2025-05-16
 ***********************************************/

#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define endl '\n'

int main() {
    fastio;
    int t;
    cin >> t;
    while(t-- > 0){
        int l;
        cin >> l;
        cin.ignore(numeric_limits<streamsize>::max(), '\n');
        string s;
        getline(cin, s);
        stack<char> ops;
        vector<char> backresult;
        for(int i = 0; i < l; ++i){
            if(s[i] )
        }
    }
    return 0;
}